CREATE DOMAIN dm_desc50 varchar(50);

CREATE TABLE musicas(
    id serial primary key,
    titulo dm_titulo,
    duracao 

)

CREATE TABLE artistas(
    id serial primary key,
    nome dm_titulo,
)

CREATE TABLE albuns(
    id serial primary key,
    
)

CREATE TABLE musicas(
    id serial primary key,
    
)